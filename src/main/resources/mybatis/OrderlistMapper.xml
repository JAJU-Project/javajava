<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="last.project.store.mapper.OrderlistMapper">
    <insert id="insertAll" parameterType="OrderlistVo">
        insert into orderlist values(O_SEQ.NEXTVAL,#{scode},#{mname},
        #{olcount},sysdate,#{ospot},#{sname},
        #{mprice},default,sysdate,#{kid})
    </insert>
</mapper>